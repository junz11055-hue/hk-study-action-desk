# AI 留学管家：产品需求文档（PRD）v0.2

> 文档状态：已于 2026-08-30 经产品负责人整体批准  
> 文档日期：2026-08-29  
> PRD 版本：v0.2；当前产品版本：v0.1 合成数据验证版  
> 产品愿景：面向内地背景赴港学生的 AI 留学通知行动中心  
> 配套材料：`00-decision-log.md`、`01-agent-architecture-v0.1.md`、`02-pilot-evidence-plan.md`、`03-pilot-acceptance-metrics-v0.1.md`

本次 v0.2 只收口产品定义，不授权修改产品代码、调用模型、接入邮箱或日历、处理真实学生数据、部署或上线。相比 v0.1，本版冻结了原生重要标记的处理方式、“要处理”的准确定义、渐进式课程画像、主动管理状态、通知策略和分阶段样本门禁，并补充 AI 决策契约与合成产品验收夹具要求。

## 0. 一句话说清楚

**AI 留学管家把难懂、分散、容易错过的学校通知，变成有原文依据的简体中文行动卡，告诉香港留学生“发生了什么、与我有什么关系、何时前要做什么”。**

当前 v0.1 只验证这件事是否能由真实 AI 安全完成：使用邀请码进入本地 Web 演示，由 DeepSeek 处理服务器内置的合成学校邮件，再由 Harness（模型外面的系统安全外壳）校验事实、日期和证据。**当前不接真实邮箱、不接真实学生数据、不写真实日历、不部署，也不存在公开访问地址。**

## 1. 产品背景

### 1.1 用户问题

内地背景的香港留学生在 offer、注册、抵港和开学初期会同时面对三类信息负担：

1. 学校通知分散在英文或繁体邮件、附件和不同部门中；
2. 用户不仅需要“看懂”，还需要判断是否与自己有关、要做什么、什么时候截止；
3. 电话卡、八达通、银行卡、香港身份证等生活事项有明显阶段性，但可靠信息来源分散、容易过期。

### 1.2 现有替代方式

用户通常依赖：

- 被动刷新邮箱；
- 把邮件复制到翻译软件或聊天机器人；
- 手工把日期记到备忘录或日历；
- 在微信群、小红书、论坛和学校网页之间反复搜索；
- 依靠同学提醒。

这些方式分别解决了“阅读”或“搜索”，但没有形成从通知到行动的可信闭环。

### 1.3 产品机会

产品不应只是邮箱聚合器或翻译器，而应把“被动查看信息”变成“主动管理事项”：

```text
学校通知 → AI 理解 → 证据校验 → 行动卡 → 提醒/追问 → 用户完成管理动作
```

## 2. 产品目标

### 2.1 长期目标

帮助赴港学生在重要截止日期前看见、理解并管理真正与自己有关的学校事项，同时在正确入学阶段获得有来源的香港生活指南。

产品首先要解决的不是“把所有学校邮件都翻译一遍”，而是从大量通知中优先找出两类信息：与用户本人专业/课程直接相关的消息，以及虽然不属于专业课程、但会影响缴费、注册、签证、学籍或账号安全的高后果事务。

### 2.2 当前 v0.1 目标

在不使用真实学生数据的前提下，验证以下最小价值闭环：

1. 真实模型能把英文或繁体合成邮件转换为简体中文行动卡候选；
2. Harness 能阻止模型编造行动、日期和证据；
3. 用户能快速理解核心结论、行动、截止时间、原文依据与不确定项；
4. 用户能围绕单封通知进行受限追问，并预览日历事件；
5. 模型不可用时，产品能诚实降级，不能把预置内容冒充 AI 结果。

### 2.3 长期北极星指标

**重要事项截止前管理率**：

> 已验证的重要事项中，用户在截止前打开行动卡并至少完成一项符合本 PRD 口径的核心管理动作的比例；稍后和打开官方入口只作弱行动单列。

“已验证的重要事项”在该指标中不是主观标签，而是冻结的 `north_star_eligible=true` 集合：事项首次通过验证时属于“要处理”，适用性已确认，义务为 `mandatory` 或条件已满足的 `conditional_mandatory`，来源与行动渠道通过安全门，并有已确认截止时间。无强制行动的关键变更和高后果未决信息不进入该分母，另看“必须看邮件查看率”。

`north_star_eligible` 一旦成立便作为历史资格保留，不能通过事后改写来美化指标；指标结算另使用 `north_star_maturity_status=pending / mature_included / excluded_cancelled_before_due`。若在当时有效的截止时间前收到可信的取消/失效事实，且当前事项已不再需要完成，则标记为 `excluded_cancelled_before_due`，不进入分子或分母但保留审计记录；截止后才收到取消/失效事实，不得追溯排除。截止被可信更新时，同一逻辑事项只计一次，并以当前版本的已确认截止结算，旧版本不得重复计数。

本 PRD 批准的未来试点目标基线为 **≥60%**。加入日历只代表用户管理了事项，不代表学校任务已经完成；真实试点启动仍需单独批准。

## 3. 目标用户

### 3.1 核心用户

- 内地背景、即将或刚刚赴港的学生；
- 首批聚焦当届授课型研究生；
- 处于 offer 接受、签证准备、抵港、注册或开学初期；
- 主要阅读简体中文，但学校信息可能是英文、繁体或混合语言；
- 每天查看通知，重要事项处理频率不固定但后果较高。

### 3.2 首期试点画像

- 30–100 名邀请制用户；
- 未来覆盖 2–3 所香港高校；
- 用户可能尚未获得学校邮箱，因此阶段指南必须能独立使用；
- 真实试点学校和首个邮箱供应商仍需在进入真实邮箱阶段前确认，不阻塞当前合成数据版 PRD。

### 3.3 核心用户任务

当用户收到或查看一封学校通知时，希望快速知道：

1. 这是否与自己的专业、培养项目、年级或已选课程直接相关；
2. 即使不属于专业课程，是否会影响缴费、注册、签证、学籍、考试资格或账号安全；
3. 这是必须处理、课程更新、普通行政还是校园资讯；
4. 具体要做什么；
5. 截止时间和时区是什么；
6. 系统为什么认为它与自己有关；
7. 原文依据在哪里，哪些地方仍不确定；
8. 下一步可以如何管理。

## 4. 产品原则

1. **AI 必须承担核心价值**：理解、翻译、相关性判断、摘要、行动与日期候选、解释和追问，不能只作为营销标签。
2. **首页不是空白聊天框**：默认展示行动卡；对话附着在具体通知或指南上。
3. **高影响结论必须有证据**：日期、金额、必须/禁止、材料和后果必须能回到原文。
4. **模型不是事实数据库**：日期归一、证据回源、权限和副作用由 Harness 决定。
5. **无法确认就明确说不确定**：模型自报分数不决定真伪；缺证据、画像不足或存在冲突的字段必须标为“可能 / 无法确认”，不得包装成确定事实。
6. **用户控制外部副作用**：未来日历写入必须先展示预览并逐次确认。
7. **指南必须有来源和有效期**：AI 只解释已审核资料，不凭空生成身份证、签证、银行等高风险结论。
8. **数据最小化**：模型只获得当前任务需要的数据，凭证永不进入模型。
9. **个人相关性优先，但后果具有否决权**：专业和课程消息默认优先于普通全校资讯；缴费、注册、签证、学籍、考试资格和账号安全等高后果事项拥有保护下限，不能被专业偏好压低。
10. **尊重但不迷信原生重要信号**：用户星标、邮箱供应商重要标记和发件人感叹号必须分开保留。它们保证邮件至少首次被分析、总结和突出展示，但不自动等于“必须做”“官方可信”“立即推送”或永久置顶。
11. **主题不决定优先级**：主题标签只回答“邮件在谈什么”；行动、后果、适用性、可信度和时效共同决定“现在是否需要处理”。必修迎新可以比普通课程资讯更重要，带感叹号的账号邮件也可能是钓鱼。
12. **低摩擦不等于零交互**：不要求用户逐封确认 AI 判断，但保留少量高信息增益的操作，例如一次性补充项目画像、批量确认课程候选和明确建立长期规则。

### 4.1 关键术语

| 术语 | 本 PRD 中的准确含义 |
|---|---|
| Harness | 模型外面的系统安全外壳，负责数据范围、事实校验、权限、失败处理和外部动作 |
| AI 行动卡 | 模型提出候选、Harness 校验后展示的通知理解结果，不是模型原文直出 |
| 已管理 | 指标层的派生结果：用户完成了符合口径的核心管理动作；不是与“稍后 / 完成”并列的单一数据库状态，也不等于学校手续已经完成 |
| 已完成 | 用户主动确认真实事项已经完成；不能由“加入日历”自动推断 |
| 学校相关 | 内容与学校事务相关，不代表发件人、链接或附件一定安全 |
| 目标 5 分钟内 | 未来试点的处理目标，不是绝对实时承诺；同步延迟必须对用户可见 |
| 简体摘要 | 当前 v0.1 自动生成的核心理解，不等于完整全文翻译 |
| 一键同步 | 未来先展示预览、再由用户明确确认的一次操作，不是后台自动写入 |
| 必须看 | 产品保证首次分析、提供摘要并让用户有清晰可见入口；不承诺用户已经实际阅读，也不代表永久置顶 |
| 必须做 | 原文对当前用户提出强制或满足条件后强制的行动，适用性、来源安全、义务属性和证据均通过校验；自愿报名按钮不属于“必须做” |
| 关键变更 | 与用户相关的重要时间、地点、规则、课程或考试变化，但邮件没有要求用户完成一个外部动作 |
| 后果等级 | 不处理或漏看可能造成的实际影响，使用高 / 中 / 低 / 未知四态，不使用不透明总分 |
| 处理分区 | 系统根据当前事实分配的“要处理 / 优先阅读 / 其他通知”，与用户自己的处理进度分开 |
| 阅读状态 | 未读 / 已读，只表示用户是否看过核心结论 |
| 管理状态 | 活跃 / 稍后 / 已安排 / 已完成 / 与我无关，由用户行为改变 |
| 事项事实状态 | 有效 / 已取消 / 已失效，由已验证的邮件或官方事实改变；“已更新”是版本关系，不是当前事项有效性状态；仅仅超过截止时间属于“已逾期”，不自动代表事项失效 |

### 4.2 个性化优先级规则

产品不使用一个不透明的“AI 总分”直接排序，而是把不同判断维度分开：

| 维度 | 回答的问题 | 用户可见结果 |
|---|---|---|
| 原生重要信号 | 发件人、邮箱供应商或用户是否已经标记重要 | 发件人标记重要 / 邮箱标记重要 / 你已星标 |
| 处理分区 | 现在是否存在适用于用户的强制行动，还是只需优先知晓 | 要处理 / 优先阅读 / 其他通知 |
| 主题 | 邮件在谈什么 | 专业与课程 / 缴费与资助 / 注册与学籍 / 签证与身份 / 考试与成绩 / 账号安全 / 校园活动 / 住宿与校园生活 / 其他校务资讯 |
| 本人相关性 | 为什么适用于这个学生 | 本人、课程、专业/项目、届别、院系、全校、无法确定或不适用 |
| 后果等级 | 漏看或不处理会造成什么影响 | 高 / 中 / 低 / 未知，并显示原因 |
| 来源可信度 | 发件人和链接是否有独立可信依据 | 官方来源已验证 / 来源未验证 / 可疑 |
| 信息完整度 | 当前能安全得出多少结论 | 信息完整 / 日期未写清 / 适用范围未写清 / 附件未解析 / 存在冲突 |
| 阅读状态 | 用户是否已经看到核心结论 | 未读 / 已读 |
| 管理状态 | 用户准备怎样管理这件事 | 活跃 / 稍后 / 已安排 / 已完成 / 与我无关 |
| 事项事实与版本 | 当前事项是否仍有效，以及是否已被新版本替代 | 有效 / 已取消 / 已失效；旧版本可标“已被更新”，截止已过另显示“已逾期” |

默认首页按处理分区展示活跃事项，每封邮件只进入一个最高分段，避免重复。原生重要信号的保护下限约束系统分类，但用户主动设为稍后、已安排、已完成、与我无关或归档后，可以离开活跃首页；这不会替用户取消星标或删除原始信号。重复、被更新覆盖和取消属于事项关系/事实状态，不构成第四个首页分区。

| 顺位 | 首页分段 | 进入条件 | 默认通知方式 |
|---:|---|---|---|
| 1 | 要处理 | 已确认适用于当前用户，原文行动属于强制或条件满足后的强制要求，且来源与安全校验足以支持用户执行；没有日期不影响进入，高后果和临近截止影响内部排序 | 按通知策略决定是否即时提醒；自愿 CTA、AI 建议和来源未验证的高风险行动不能进入 |
| 2 | 优先阅读 | 关键变更；专业/课程直接相关但无明确行动；原生重要标记首次出现；或可能高影响但适用范围、日期、来源或附件仍不完整 | 确保生成摘要并提供醒目入口；不确定项直接展示，不强迫用户先回答 |
| 3 | 其他通知 | 无明确行动的一般行政、活动、新闻和推广 | 默认进入每日摘要或仅通知中心 |

另设独立的列表可见状态 `active / read_folded / user_hidden / merged`。重复提醒合并到原事项；已更新内容保留版本但只展示新事实；用户完成、设为无关或主动归档后从活跃首页移出。上述结果不属于首页第四分区。

疑似钓鱼、索取密码/验证码或账号风险使用独立“安全提醒”标签，可提升展示等级，但不能因此把可疑邮件包装成官方要求。

Harness 先分别确定安全与来源、用户适用性、明确行动、后果、关键变更、日期和原生重要信号，再裁决处理分区与通知方式。安全风险不能被“官方口吻”或感叹号覆盖；主题标签不能直接改变分区。同等后果与时效下，排序为本人课程/班级 > 本人专业/项目 > 本院系 > 全校通用。专业相关性是强加权，但不能压低缴费、注册、签证、学籍、考试资格和账号安全等高后果事项。

任何邮件都必须展示“为什么排在这里”的依据，不能只给出一个分数。更新、延期或取消邮件必须更新原事项并优先于旧通知，不能生成彼此冲突的重复卡片。

原生重要信号必须区分来源，不能合并成一个模糊的“AI 认为重要”：

| 信号来源 | 保护规则 | 允许降级的条件 |
|---|---|---|
| 用户主动星标或标记重要 | 活跃事项在用户取消前至少进入“优先阅读” | 用户可以主动将事项移出活跃首页，但系统不能替用户取消星标或把它改成“其他通知” |
| 邮箱供应商重要标记 | 首次必须分析、总结并至少进入“优先阅读” | 用户对当前邮件纠错，或明确批准针对该来源/类型的长期规则 |
| 发件人高重要性或感叹号 | 首次必须分析、总结并至少进入“优先阅读” | 安全策略仍可改为醒目的“安全风险”；用户可纠错或批准长期规则 |
| AI/Harness 内容判断 | 不属于原生信号，必须展示具体依据 | 新证据、邮件更新或用户纠错可以改变结果 |

“首次保证可见”不等于立即推送或永久置顶。钓鱼邮件即使带感叹号，也只能显示为“发件人标记重要 + 安全风险”，不能包装成官方要求。

#### 4.2.1 优先级冲突验收矩阵

以下矩阵是设计、研发和测试必须得到同一答案的最小产品真值表。通知方式描述未来试点行为；当前合成版不发送真实推送。

| ID | 场景 | 来件处置 `incoming_disposition` | 结果事项分区 `home_section` | 关键标签 | 默认通知 | 必须提炼/解释 |
|---|---|---|---|---|---|---|
| M01 | 来源安全已验证、已确认适用的学费缴纳，含明确截止和逾期后果 | `new_item` | 要处理 | 缴费与资助 + 高后果 | 即时 | 金额、对象、截止、官方核验入口、逾期后果和逐字证据 |
| M02 | 来源安全已验证、已确认适用的注册材料补交，没有写明日期 | `new_item` | 要处理 | 注册与学籍 + 日期未写清 + 高后果 | 即时 | 明确行动、材料、后果，并说明邮件没有给出截止时间；不得生成日历日期 |
| M03 | 签证通知可能适用，但届别或对象条件缺失，且没有 7 天内明确日期 | `new_item` | 优先阅读 | 签证与身份 + 适用范围未写清 + 高后果 | 每日摘要 | 已知要求、缺失条件、官方来源；不得断言“你必须办理” |
| M04 | 来源安全已验证、本人课程作业或选课含强制提交动作和截止 | `new_item` | 要处理 | 专业与课程 | 截止 ≤72 小时即时，否则摘要 | 课程匹配依据、义务属性、动作、对象、截止和后果 |
| M05 | 系统没有可唯一匹配的旧事项，但邮件自身明确说明本人课程时间或教室改变，且没有要求回复 | `new_item` | 优先阅读 | 专业与课程 + 关键变更 | 即时 | 新旧时间/地点、影响对象和更新证据；不得虚构“请确认”待办 |
| M06 | 系统没有可唯一匹配的旧事项，但邮件自身明确说明本人考试日期、规则或地点改变，且没有外部动作 | `new_item` | 优先阅读 | 考试与成绩 + 关键变更 + 高后果 | 即时 | 新旧信息、课程、考试类型、时区和证据 |
| M07 | 与本人专业直接相关的讲座或学术资讯，无强制行动 | `new_item` | 优先阅读 | 专业与课程 | 每日摘要 | 为什么相关、是否自愿、时间地点；不得写成必修 |
| M08 | 发件人用感叹号标记的自愿校园活动 | `new_item` | 优先阅读 | 校园活动 + 发件人标记重要 + 首次保护 | 每日摘要 | 活动内容、是否报名、是否自愿，并解释进入本区仅因原生标记 |
| M09 | 邮箱供应商标记重要的普通通讯 | `new_item` | 优先阅读 | 邮箱标记重要 + 首次保护 | 每日摘要 | 核心内容和重要标记来源；用户纠错后可降级 |
| M10 | 用户主动星标的普通资讯 | `new_item` | 优先阅读 | 你已星标 | 每日摘要 | 核心内容；保持到用户取消星标 |
| M11 | 带感叹号并索取密码、验证码或来源未验证的付款邮件 | `new_item` | 优先阅读 | 账号安全/缴费与资助 + 发件人标记重要 + 来源未验证/安全风险 | 即时安全提醒 | 风险原因、可信核验方式；禁止包装成官方行动、回复凭证或直接付款 |
| M12 | 官方账号安全通知明确要求当前用户重置凭证 | `new_item` | 要处理 | 账号安全 + 高后果 | 即时 | 受影响服务、官方核验渠道、截止和安全注意事项 |
| M13 | 面向全校的自愿社团或推广活动，无重要标记 | `new_item` | 其他通知 | 校园活动 | 每日摘要 | 是否自愿、报名时间、费用/名额；不强调专业相关 |
| M14 | 来源安全已验证、本项目强制参加并要求报名的迎新活动 | `new_item` | 要处理 | 校园活动 + 专业/项目相关 | 截止 ≤72 小时即时，否则摘要 | 强制依据、适用项目、行动、时间地点和后果 |
| M15 | 正文只说明附件可能含关键要求，但附件尚未解析 | `new_item` | 优先阅读 | 附件未解析；按主题增加标签 | 每日摘要 | 正文已知事实、附件缺口和回原邮箱入口；不得猜附件内容 |
| M16 | 更新、延期或取消已存在的课程/考试关键安排，且没有新增外部动作 | `update_existing` | 优先阅读 | 关键变更 + 日期已更新/已取消 | 即时 | 关联旧事项、新旧差异和证据；更新原事项并重新置为未读 |

M05/M06 只覆盖“邮件描述了变化，但系统无法按 REL02–REL04 唯一命中旧事项”的首次建项；一旦满足 REL02–REL04，必须统一走 M16 的 `update_existing`，不得因为主题属于课程或考试而另建当前事项。

#### 4.2.2 通知策略

处理分区与通知渠道是两个独立决策。原生重要标记单独出现时只保证首次突出展示，不自动触发即时推送。以下为默认且可测试的基线；用户未来可修改提醒偏好，但不能突破安全和首页可见性下限。

- **时间阈值**：`临近截止` 指香港时间下距离已确认硬截止不超过 72 小时且尚未过期；`时间敏感的可能高后果` 指邮件有可验证业务日期且不超过 7 个自然日。日期缺失不能命中时间阈值。
- **即时提醒**：已确认适用的高后果强制行动；72 小时内到期的其他强制行动；已确认课程/考试时间、地点、规则或取消的关键变更；官方账号安全强制行动；疑似索取密码、验证码或异常付款的安全风险；7 天内可能适用的高后果事项。
- **每日摘要**：超过 72 小时的非高后果强制行动；专业/课程直接相关但无行动的资讯；只有原生重要标记但不满足即时条件的邮件；自愿校园活动；没有明确日期且只“可能适用”的高后果信息。
- **仅通知中心**：普通新闻、推广和低价值资讯。用户长期降级规则只在当前邮件不满足高后果、强制行动、关键变更、安全风险、临近截止或原生重要保护条件时生效。
- **升级与更新**：活跃强制行动首次跨入 72 小时窗口时最多补发一次即时提醒；更新、延期或取消若影响现有要处理事项、课程/考试关键安排，或旧版本已单封推送，则再次即时提醒，否则进入摘要。
- **稍后恢复**：用户设定的稍后时间到达后，管理状态恢复为“活跃”、阅读状态改为“未读”，并按用户当时选择的提醒方式触发一次；不自动标为已安排或已完成。
- **免打扰**：尊重用户和操作系统设置，不绕过免打扰；即时提醒排队到免打扰结束。系统可以在行动中心保持醒目，但不能虚假声称推送已送达或用户已看见。
- **合成验收默认值**：固定香港当前时间、默认通知偏好和“免打扰关闭”；通知渠道由 Harness 确定性规则验收，不使用模型输出决定。

#### 4.2.3 来源与行动渠道信任真值表

来源可信度与行动渠道安全是 Harness 的确定性裁决，不交给模型。`source_status=official_verified` 至少要求连接器认证结果通过，且真实发件地址/学校租户或经审核服务商命中有版本的允许映射；显示名、Logo、学校口吻、感叹号或仅有学校相似域名都不够。涉及付款、凭证、证件或账号操作时，还必须独立验证最终链接域名、回复地址或办理渠道。

| ID | 已验证事实 | `source_status` / `action_channel_status` | 产品裁决 |
|---|---|---|---|
| SRC01 | 连接器认证通过；真实发件地址或学校租户命中允许映射；所需办理渠道为已验证官方渠道；无安全冲突 | `official_verified / verified` | 适用性与义务也通过时，可以支持“要处理” |
| SRC02 | 经审核的学校服务商通过认证，且服务商—学校—业务范围映射与最终办理域名均匹配 | `official_verified / verified` | 在映射业务范围内等同受控官方来源；超出范围重新判定 |
| SRC03 | 认证通过且使用学校域名，但具体租户、发件身份或业务角色未进入允许映射 | `unverified / unverified` | 可以摘要；有强制或高风险行动时进入“优先阅读 + 先核验”，不能进入“要处理” |
| SRC04 | 只有显示名、Logo、官方口吻、感叹号或相似域名，没有独立认证/映射证据 | `unverified / unverified` | 不得显示“官方已验证”；按内容与原生重要保护展示 |
| SRC05 | 发件来源可验证，但付款/凭证/证件/账号操作指向未知域名、未知回复地址或不可解释重定向 | `official_verified / unverified` | 保留来源事实但阻断行动链接、要处理和日历资格；进入“优先阅读 + 先核验” |
| SRC06 | 认证失败、仿冒域名、索取密码/验证码、异常付款或已知恶意渠道 | `suspicious / suspicious` | 醒目安全提醒；不得包装成官方行动或建议用户提交信息 |
| SRC07 | 无外部办理渠道，行动只要求在已确认时间地点出席或携带材料 | `official_verified / not_required` | 来源、适用性和义务通过时可以支持“要处理”；不得虚构链接 |

同一邮件可同时是“来源官方已验证”但“行动渠道未验证”。任何 `source_status` 或 `action_channel_status` 缺失都不能按 `false` 或“安全”处理；涉及高风险动作时按未验证关闭。`要处理` 必须满足 `source_status=official_verified`、`action_channel_status=verified/not_required`、安全状态无冲突，以及适用性和义务条件全部通过。

#### 4.2.4 同一事项、更新与重复匹配真值表

错误覆盖旧事项比多显示一张待核验卡后果更大，因此证据不足时默认不合并。`incoming_disposition` 只允许 `new_item / exact_duplicate_suppressed / update_existing`：

| ID | 匹配证据 | `incoming_disposition` | 必须行为 |
|---|---|---|---|
| REL01 | 供应商消息 ID 相同，或规范化内容指纹与附件指纹均相同 | `exact_duplicate_suppressed` | 不新建卡、不新建版本、不重复通知，只记录重复事件 |
| REL02 | 明确引用旧消息/事项 ID，且学校、业务对象和稳定业务键一致，并给出新日期/地点/规则 | `update_existing` | 创建原事项新版本，展示差异并重新裁决阅读、管理和通知状态 |
| REL03 | 同线程，且课程/项目/考试/缴费批次等稳定业务键与行动对象一致，正文明确表示更正、延期或替换 | `update_existing` | 与 REL02 相同；主题相似本身不算稳定业务键 |
| REL04 | 明确取消，且引用关系或稳定业务键能唯一命中旧事项 | `update_existing` | 原事项新版本标记取消，保留历史并处理既有安排预览；重新置未读并在用户看到取消前保持活跃 |
| REL05 | 只有相同/相似主题、发件人或课程代码，没有同一行动对象或稳定业务键 | `new_item` | 新建事项；不得覆盖旧事实 |
| REL06 | 同一课程但属于不同作业、考试、报名或不同截止事项 | `new_item` | 分别保留；可显示课程关联但不能合并生命周期 |
| REL07 | 证据冲突、可能相关但不能唯一命中 | `new_item` | 新建并标“可能关联旧事项”，不得继承旧日期、管理状态或日历关系 |

更新、延期和取消都必须通过 REL02–REL04 之一；否则不能覆盖旧事项。精确重复使用 REL01，不得与“有新事实的更新”混为一类。

#### 4.2.5 后果等级与同区排序真值表

后果等级回答“漏看或不处理会发生什么”，不能从主题、感叹号或金额大小单独推断。没有证据时使用 `unknown`，不能为了排序方便默认为低：

| ID | 可验证后果 | 等级 | 边界规则 |
|---|---|---|---|
| CON01 | 可能失去入学/注册/学籍/签证/考试资格，产生无法保证补救的重要财务损失，或造成账号/凭证安全事件 | `high` | 必须有具体后果证据；“很重要”或主题属于缴费/签证本身不够 |
| CON02 | 官方明确存在可用补办/宽限，但不处理仍会造成可恢复的延迟、少量罚款、名额/服务损失或明显行政成本 | `medium` | 只有补救入口真实、期限有效且不会先失去资格时才能从 high 降为 medium |
| CON03 | 已确认的一般课程/行政变更需要优先知道，但没有强制动作、资格/安全风险或明显损失 | `medium` | 关键变更仍可即时提醒；medium 不等于普通通知 |
| CON04 | 纯自愿活动、一般资讯或推广，不参与不会产生实质损失 | `low` | 有专业直接相关或原生重要保护时仍可进入优先阅读 |
| CON05 | 邮件暗示可能有后果，但附件、对象、来源或具体影响缺失 | `unknown` | 若存在高后果线索，内部标记 `unknown_with_high_consequence_clue=true`，保持优先阅读并显示缺口；不得按 low 降级 |
| CON06 | 一般账号安全建议，无当前账号受影响证据、无强制动作 | `medium` | 已验证的账号泄露、强制重置或凭证风险按 high；钓鱼另走安全状态 |

首页仍只有三个处理分区。每个分区内部使用固定排序键：①未读安全风险、取消/延期/关键更新；②已逾期及距离已确认截止的时间窗口；③按内部 `consequence_sort_bucket` 的固定顺序 `confirmed_high > unknown_high_clue > confirmed_medium > unknown_other > confirmed_low`；④本人课程/班级 > 本人专业/项目 > 本院系 > 全校；⑤用户星标、供应商/发件人首次重要保护；⑥接收时间。`unknown_with_high_consequence_clue=true` 映射到 `unknown_high_clue`，其他 `unknown` 映射到 `unknown_other`；用户可见后果仍为四态中的“未知 + 具体缺口”，不伪装成已确认高后果。前一键相同时才比较后一键，不能用一个不透明总分覆盖该顺序。

### 4.3 低摩擦确认原则

信息不足不等于立即弹窗询问用户。默认行为是先展示摘要、已知事实和“哪些信息仍不确定”，让用户可以直接阅读。

以下场景不强制确认：

- 展示行动卡、主题标签和相关性解释；
- 把高重要性或专业相关邮件放入“优先阅读”；
- 展示“可能与你相关”“日期待核对”或“附件未解析”；
- 将低风险资讯放入摘要。
- 首次使用时只要求未来真实试点所需的学校、专业/培养项目和届别；课程清单可选，不阻断进入首页。
- AI 从邮件提出课程候选时，以设置页中的批量建议展示，不逐封弹窗，也不能自动写成已确认课程。

只有以下场景才要求用户明确确认：

- 使用不确定日期创建或修改日历事件；
- 永久忽略同类邮件，或让系统记住“这不是我的课程/专业”；
- 把“已管理”改为“学校事项已完成”；
- 任何会产生外部写入、长期规则变化或高影响不可逆后果的动作。

纠错入口采用卡片上的可选一键反馈，不阻断用户阅读。用户没有反馈时，系统继续保守展示，不反复追问。

### 4.4 多标签与分类提炼模板

邮件允许同时拥有多个标签。例如一封不要求用户回复的期末考试改期邮件可以同时标记为“专业与课程 + 考试与成绩 + 关键变更 + 优先阅读”。主题标签不能互相排斥，也不能替代处理分区、后果等级、阅读/管理状态或事项事实状态。

| 主题标签 | 行动卡必须优先提炼的信息 |
|---|---|
| 专业与课程 | 课程代码/名称、项目或届别、影响对象、课程/课表变化、所需行动、时间地点、原文依据 |
| 缴费与资助 | 金额与币种、付款对象、截止时间、官方入口或核验方式、完成标准、逾期后果、原文依据 |
| 注册与学籍 | 办理事项、适用资格、步骤、所需材料、截止时间、不处理后果、原文依据 |
| 签证与身份 | 办理类型、发布机构、适用对象、所需材料、预约/截止时间、官方来源、不确定项 |
| 考试与成绩 | 课程、考试/考核类型、日期时间与时区、地点、允许材料/规则、更新或取消、后果 |
| 账号安全 | 受影响服务、风险内容、官方核验渠道、截止时间、是否索取凭证、安全警告；绝不建议回复密码或验证码 |
| 校园活动 | 主办方、活动内容、适用对象、是否报名、时间地点、费用/名额、是否自愿参加 |
| 住宿与校园生活 | 办理事项、适用对象、材料、费用、日期、地点、官方来源和是否必须 |
| 其他校务资讯 | 事项类型、发布部门、适用对象、是否需要行动、时间范围、来源和不确定项 |

所有提炼字段都必须区分“邮件明确写明”“系统根据画像匹配”“AI 建议”和“不确定”，不能混成一段没有来源的摘要。

## 5. 版本范围

| 能力 | 当前 v0.1 合成数据版 | 未来邀请制试点 | 当前是否获准 |
|---|---|---|---|
| 邀请码 | 本地演示会话 | 邀请资格 + 稳定账户 | 当前仅本地版获准 |
| 学生画像 | 服务器内置合成画像 | 首次填写学校、专业/培养项目和届别；学期可确认建议值，课程可跳过并后补/批量确认候选 | v0.1 合成现状已完成；v0.2 画像改造及真实画像未获准 |
| 邮件来源 | 6 封服务器内置合成邮件 | 一个主要供应商的只读 OAuth 连接 | 真实邮箱未获准 |
| AI 模型 | DeepSeek 单供应商；此前单样本 smoke 已获准并完成 | 生产供应商需重新做数据与成本准入 | 本轮不授权任何新模型调用 |
| AI 行动卡 | v0.1 基础卡支持 | 支持 v0.2 完整字段与状态 | v0.2 改造未获准 |
| 通知追问 | 三个固定问题 | 围绕当前通知的受限追问 | 固定问题获准 |
| 附件 | 不接收真实附件 | PDF 优先；ICS 先作为实验能力 | 未获准 |
| 推送 | 页面内状态 | Web/PWA 即时通知 + 每日摘要 | 未获准 |
| 日历 | 只生成本地预览 | 用户确认后幂等写入 | 写入未获准 |
| 阶段指南 | 合成演示内容 | 有来源、版本、审核人与有效期的内容 | 演示版获准 |
| 部署 | 不部署、仅本机 | 封闭邀请制环境 | 未获准 |

## 6. 当前 v0.1 核心闭环

1. 用户输入邀请码进入本地演示会话；
2. 系统展示合成学生画像与合成通知；
3. 用户选择一封通知并触发分析；
4. DeepSeek 生成结构化理解候选；
5. Harness 校验 Schema、证据、日期角色、语义和安全风险；
6. 通过后展示 AI 行动卡；失败时展示明确标记的预置结果或失败状态；
7. 用户查看原文、简体摘要、行动、日期、不确定项和证据；
8. 用户可选择三个固定问题之一追问；
9. 日期可靠时可生成本地日历预览，但不能写入任何真实日历。

## 7. 产品功能需求：v0.1 现状与 v0.2 目标

优先级定义：P0 为后续实现 v0.2 行为时必须满足，P1 为增强项；优先级不是“当前代码已经完成”的证明。FR-01、FR-05、FR-07 至 FR-10 已有 v0.1 基础实现，但本节的新语义尚未获准修改代码；FR-02、FR-03、FR-04/04A、FR-06 和 FR-11 含明确的 v0.2 目标差异。真实邮箱、推送和日历仍只在第 8 节描述未来方向。

### FR-01 邀请码进入

**优先级：P0**

- 用户必须先输入邀请码才能进入演示；
- 邀请码无效、过期或用尽时使用一致错误信息；
- 成功后建立独立本地会话；
- 邀请码不得进入浏览器日志、普通日志或模型上下文；
- 当前邀请码不等同于生产级账户身份。

**验收：**

- 无有效会话不能访问通知详情或分析接口；
- 登录后会话 Cookie 具备必要的浏览器安全属性；
- 登出后会话立即失效。

### FR-02 合成用户画像

**优先级：P0**

- 展示明确标记为“完全合成”的学校、课程、入学阶段和时区；
- 画像用于判断通知是否适用于当前用户；
- 当前版本不允许输入真实姓名、学校邮箱、学号或证件信息。
- 为验证优先级，合成画像需要包含学校、培养项目/专业、届别、学期，以及至少一门带课程代码、标准名称、别名和确认状态的课程；这不代表授权采集真实课程数据。
- 合成课程也必须区分 `confirmed / candidate / removed / expired`，用于验证“候选或过期画像只能提高可见性，不能证明用户必须行动”。

### FR-03 合成通知中心

**优先级：P0**

- 当前已实现界面只展示服务器白名单中的 6 封合成通知摘要；
- 发件地址必须使用不可投递的 `.invalid` 域名；
- 客户端只能提交通知 ID，不能提交任意邮件正文；
- 覆盖英文、繁体、截止事项、日期更新、普通资讯和安全攻击样本。
- 新优先级规则在宣称实现前，必须建立 48 个合成产品验收夹具，其中 32 个开发/回归、16 个锁定回归；当前 6 封样本只证明原有演示链路可运行，不能证明 v0.2 决策正确。
- 单人确认的 48 例只能验证实现是否符合产品合同，不能称为独立金标或证明 AI 统计优势。夹具至少覆盖 4.2.1 的全部冲突场景，以及画像缺失/过期/移除、重要标记来源、专业相关无行动、非专业高后果、更新取消、日期冲突、可疑来源和中英繁混合语言；本次只定义文档，不授权新增样本文件或运行模型。

### FR-04 AI 行动卡

**优先级：P0**

每张通过校验的行动卡至少包含：

- 与用户的相关性；
- 邮件原生重要信号及其来源；
- 适用范围：本人、已确认课程、专业/培养项目、届别、院系、全校、无法确定或不适用；
- “为什么与你相关”的可读依据；
- 处理分区：要处理、优先阅读或其他通知；
- 主题标签：专业与课程、缴费与资助、注册与学籍、签证与身份、考试与成绩、账号安全、校园活动、住宿与校园生活、其他校务资讯，可多选；
- 来源可信度和信息完整度；
- 安全、日期更新、附件未解析等风险/可信状态标签；
- 后果等级及具体原因；
- 阅读状态、管理状态和事项事实状态，与处理分区分开；
- 简体中文标题和摘要；
- 原子行动项；
- 每个行动的义务属性：强制、条件满足后强制、建议或自愿；
- 原始日期、香港时区归一值和置信状态；
- 原文证据；
- 风险标记；
- 不确定项；
- 当前内容来自 DeepSeek、预置逻辑还是安全策略的明确标识。

**验收：**

- 英文或繁体核心事实不得反转；
- 日期、金额、否定和“必须/建议”含义保持一致；
- 所有高影响结论都必须有逐字证据，包括行动、日期、金额、材料、资格、后果、必须/禁止、账号安全和官方入口；
- “可能 / 无法确认”或存在冲突的日期不能取得日历预览资格。

### FR-04A 个性化相关性与优先级

**优先级：P0 产品要求；已纳入 v0.2 文档基线，尚未授权修改代码实现**

模型需要分别提出以下候选，不得只返回一个模糊总分：

- 邮件适用范围；
- 命中的专业、培养项目、届别或课程代码/名称；
- 是否明确要求当前用户行动、义务属于强制/条件满足后强制/建议/自愿、是否存在关键变更、是否有硬截止；
- 不处理或漏看的潜在后果及其证据；
- 多主题候选与安全风险；
- 支撑上述判断的原文证据。

Harness 使用已确认的用户画像和规则裁决最终分组：

- 专业/课程命中必须基于课程代码、课程名称、项目名称、受控别名、对象条件或用户确认，不能只靠泛化语义猜测；
- 只有邮件本身存在可信的课程代码/名称、项目、届别、受众条件或高后果线索，而画像不足以完成适用性判断时，才进入“优先阅读”并显示具体缺口；纯新闻或推广不能仅因用户没填课程而升级；
- 高后果事务设置不可被专业偏好、用户静音或 AI 低分突破的展示下限；用户关闭推送时仍保留在行动中心，但不得虚假声称已提醒；
- 高后果事项即使没有明确日期，也不能降到“其他通知”；
- 没有明确行动的课程或考试关键变更进入“优先阅读”，可按通知策略即时提醒，但不得虚构待办；
- 自愿 CTA、建议和可选报名不构成“要处理”；只有用户另行保存或承诺参加时，才作为个人安排进入“我的事项”，不能改写成学校强制要求；
- 涉及付款、凭证、证件提交、账号操作等高风险行动时，来源未验证或可疑即进入“优先阅读 + 先核验”，不得进入“要处理”或开放行动链接；
- 更新、延期和取消优先覆盖同一事项的旧状态；
- 画像候选、缺失、过期或与邮件受众冲突时，只有存在上述相关/高后果线索才进入“优先阅读”并显示具体缺口，不能仅凭候选画像判定必须行动；
- 用户可执行“这是我的课程”“不是我的课程”“与我的专业无关”“优先级太高/太低”等纠错；
- 纠错先作用于当前用户，只有经过验证的稳定映射才能更新规则，不能把单个用户纠错直接推广给所有人。

### FR-05 DeepSeek 模型调用

**优先级：P0**

- 服务端仅使用 DeepSeek 官方 HTTPS API；
- Key 只存在本地服务端配置，不发送给浏览器；
- 使用严格结构化输出；
- 不向模型注册邮箱、日历、文件、Shell 或环境变量工具；
- 当前只允许发送完全合成数据；
- 请求超时、截断和临时错误采用有限重试，不得无限消费；
- 失败后允许降级，但必须明确标记为非 AI 结果。

### FR-06 Harness 事实与安全校验

**优先级：P0**

Harness 必须：

- 分别保留发件人、邮箱供应商和用户设置的重要标记，不能合并成不可解释的 AI 分数；
- 用户星标在用户取消前至少进入“优先阅读”；发件人或邮箱供应商重要标记保证首次至少进入“优先阅读”，之后只能根据用户纠错或经用户批准的长期规则降级；
- 不能仅凭原生重要标记自动生成行动、日期、官方认证、即时推送或永久置顶；
- 校验输出字段、类型、枚举和长度；
- 验证证据逐字存在于当前合成邮件，并检查证据在语义上支持对应结论；“引用存在”不能替代“引用支持”；
- 用确定性规则归一香港日期和时区；
- 阻止结束时间冒充开始时间；
- 阻止模型新增邮件不支持的缴费、选课或其他高影响行动；
- 把邮件正文视为不可信数据；
- 对 Prompt Injection、钓鱼和索取密码/验证码的内容失败关闭；
- 决定通知等级与日历资格，模型只能提供候选。

### FR-07 单封通知追问

**优先级：P0**

当前版本只支持三个固定问题：

1. 我具体要做什么；
2. 截止时间依据是什么；
3. 哪些地方仍不确定。

**验收：**

- 不提供开放文本框，避免用户粘贴真实邮件或个人信息；
- 回答只能引用当前通知证据；
- 安全攻击样本的追问绕过模型，使用确定性安全策略；
- 模型失败时明确展示预置回答。

### FR-08 日历预览

**优先级：P0**

- 只有通过日期和证据校验的行动才能生成预览；
- 预览展示标题、开始时间、时区、提醒、目标日历占位和日期依据；
- “模拟加入”只改变当前页面状态；
- 必须持续提示“没有写入真实日历”。

### FR-09 阶段指南

**优先级：P1**

- 按合成入学阶段展示电话卡、流量、八达通、银行卡和香港身份证等主题；
- 当前内容必须标记为演示，不能宣称已完成上线前复核；
- 未来正式内容必须包含发布机构、来源链接、审核人、版本、更新时间和有效期；
- 过期或缺少来源的高风险指南不得发布为确定事实。

### FR-10 状态与降级

**优先级：P0**

产品必须区分：

- DeepSeek 正在分析；
- DeepSeek 结果已通过 Harness；
- DeepSeek 调用失败，展示预置合成分析；
- 结果因安全策略被替换；
- 已知内容可安全展示，但日期、适用范围或附件存在具体缺口；
- 日历仅为预览；
- 会话失效，需要重新输入邀请码。

不得用统一的“AI 已完成”掩盖不同来源或失败状态。预置行动卡只允许用于明确标记的合成演示；未来真实邮件模型失败时只能展示原始邮件、原生重要信号、安全提示、具体失败状态和重试进度，不能套用预置事实。

### FR-11 主动管理状态

**优先级：P0 产品要求；已纳入 v0.2 文档基线，尚未授权修改代码实现**

- 首页处理分区、系统分析状态、`read_status`、`management_status` 和 `item_status` 必须分别保存，不能合并成一个“已处理”；列表可见状态是由这些事实、去重关系和用户归档事件计算的可重算投影，不作为另一份独立业务真相；
- 用户打开卡片只将 `read_status` 改为已读，不等于已安排或已完成；只有“已确认无需强制行动且没有未决高后果/安全/适用性/来源/附件缺口”的优先阅读卡才自动折叠，避免增加“知道了”操作；
- 用户可以设置可撤销的稍后时间，不需要额外确认；到期后恢复活跃和未读，稍后只算弱管理；
- 持久内部安排或未来真实日历写入成功后进入“已安排”，仍不代表学校事项已完成；
- “已完成”只能由用户主动标记；
- “与我无关”默认只修正当前邮件；永久忽略同类邮件或形成长期规则时才要求明确确认；
- 更新、延期或取消必须作用于原事项，保留旧事实和用户操作记录，不生成相互冲突的独立卡片；
- 合成版的模拟加入或模拟处理不得进入真实业务指标。

## 8. 未来邀请制试点需求

本节描述产品方向，**不是当前开发或数据接入授权**。

### FR-F01 稳定账户

- 邀请码只验证试点资格；
- 用户通过个人邮箱 Magic Link 或一次性验证码建立和恢复账户；
- 学校邮箱和个人日历作为两个独立连接；
- 绑定、换绑、解绑前需要近期重新认证。
- 首次只要求学校、专业/培养项目和入学届别；学期与香港时区可使用系统建议后由用户确认，课程清单可以跳过，不阻断进入行动中心。
- 每门课程至少包含课程代码、标准名称、受控别名、学期、`confirmed / candidate / removed / expired` 状态、来源、最近确认时间和有效期。
- AI 可以从多封可信邮件提出课程候选，但只能集中展示供用户批量确认，不能静默写成已确认选课事实，也不能逐封弹窗阻断阅读。
- 候选或过期课程只能作为进入“优先阅读”的辅助依据，不能证明“当前用户必须行动”；用户可以随时更新画像。

### FR-F02 学校邮箱连接

- 首期只实现一个主要供应商；
- 只申请满足增量读取需求的最小只读权限；
- 在供应商实际提供的范围内读取并保留发件人重要性、邮箱供应商重要分类和用户星标/重要标记，同时记录信号来源；
- 不发送、回复、删除、移动或标记已读邮件；
- Webhook 只作为变化信号，后台以授权身份执行增量读取；
- 订阅丢失时使用增量对账；
- 断开后立即停止新同步。

首发 Microsoft Graph 或 Gmail API 必须根据试点学校与 OAuth 前置验证决定。当前证据更支持优先调查 HKUST、PolyU 的 Microsoft 365 路径，但不能据此宣称第三方 OAuth 已可用。

### FR-F03 实时处理与通知

- 新邮件到达后 5 分钟内形成行动卡的目标达成率 ≥95%；
- 首页分区与外部通知分别决策，严格使用 4.2.2 的即时提醒、每日摘要和仅通知中心规则；
- 只有原生重要标记而没有行动、关键变更、高后果或时效条件时，不发送单封即时推送；
- 尊重用户免打扰、退订和提醒偏好；
- 推送送达不等于用户已查看。

### FR-F04 附件

- PDF 为首个正式支持类型；
- ICS 在真实锁定样本不足 20 前只作为实验能力；
- 不支持、损坏、加密、超限或危险附件必须安全降级；
- 解析失败时不得由模型猜测附件内容。

### FR-F05 真实日历同步

- 邮箱授权和日历授权分离；
- 每次写入前展示完整预览；
- 用户确认必须绑定看到的精确快照；
- 使用稳定幂等键防止重复事件；
- 写入结果未知时先查询对账，不能直接重复创建；
- 邮件更新日期时只建议更新，不静默修改用户日历。

### FR-F06 用户控制

- 用户可以纠正相关性、处理分区、主题、后果等级、行动和日期；
- 用户可以断开邮箱或日历；
- 用户可以删除账户和允许删除的数据；
- 运营人员默认不能查看邮件正文；
- 用户纠错进入审计和质量评测，但不能自动作为其他用户事实。

## 9. Agent 与 Harness 产品要求

### 9.1 唯一用户可见 Agent

首期只有一个“邮件智能 Agent”，包含两种受限模式：

| 模式 | 任务 | 外部写权限 |
|---|---|---|
| 结构化理解 | 分类、翻译、摘要、行动与日期候选、证据定位、风险识别 | 无 |
| 单封追问 | 解释当前通知、依据和未知项 | 无 |

不使用多 Agent 团队，不让模型自行扩展工具或长期目标。

### 9.2 模型负责

- 语言理解与简体表达；
- 学校事务相关性和用户适用性候选；
- 多主题、明确行动、义务属性、关键变更和后果等级候选；
- 行动、日期、证据和风险候选；
- 基于当前通知证据回答受限追问；
- 未来基于已检索指南片段进行解释。

### 9.3 Harness 负责

- 身份、邀请、连接、数据范围和租户边界；
- 邮件同步、去重、附件隔离和任务状态；
- Schema、证据、日期、时区和高影响语义校验；
- 模型超时、重试、熔断、成本和降级；
- 通知投递和免打扰；
- 所有日历副作用与用户审批；
- 数据保留、删除、审计和指标。

### 9.4 Agent 完成条件

只有同时满足以下条件，行动卡才可发布：

1. 结构合法；
2. 所有高影响结论都有可定位且语义支持的证据，包括日期、金额、材料、资格、后果、必须/禁止、账号安全和官方入口；
3. 日期已通过确定性校验，或被明确标为不确定并失去日历资格；
4. 邮件内容没有改变系统身份、规则或权限；
5. 没有把推测写成学校事实；
6. 不包含凭证、其他用户数据或内部配置；
7. 来源模式、模型版本和校验结果可追踪。

### 9.5 AI 决策契约

本契约回答“模型看见什么、可以提出什么、Harness 如何放行”。模型只提出候选，不能直接决定首页分区、外部通知、来源是否官方或日历资格。

#### 9.5.1 模型输入

当前版本只允许以下完全合成的结构化输入；未来真实试点仍需另行批准：

| 输入组 | 必须字段 | 约束 |
|---|---|---|
| 任务上下文 | `task_type`、当前香港时间、时区、语言目标、Schema 版本 | 只处理结构化理解或当前通知受限追问 |
| 用户画像 | 学校、项目/专业、届别、学期、课程列表；每个字段都有稳定 `profile_field_id`、值、来源、确认状态和有效期 | `candidate` 或 `expired` 最多支持“可能相关”；`removed` 只能作为负向/历史上下文，三者都不能当成当前已选课程事实 |
| 邮件元数据 | 通知 ID、线程/更新关联、发件人、主题、`sent_at`、`received_at`、语言 | 不把发件人显示名当作官方认证；相对日期以经过 Harness 校验的 `sent_at` 为语义锚点，缺失或不可信时不得生成确定日期 |
| 原生重要信号 | 发件人 importance、供应商 importance、用户星标分别记录 | 每项都带来源，缺失与 `false` 必须区分 |
| 内容 | 清洗后的正文、附件文本片段、页码和解析状态 | 全部是不可信数据，不能改变系统规则或工具权限 |
| 已知安全事实 | 邮件认证结果、已验证域名/来源、链接真实域名、附件风险 | 来源可信度最终由 Harness 决定 |
| 历史事项 | 已匹配旧事项及其日期、状态和证据 | 只用于识别提醒、更新、延期或取消 |

Harness 另持有不依赖模型的决策上下文：`first_seen`、当前星标、既有用户纠错、已批准长期规则、通知偏好、免打扰状态和固定策略版本。它们参与最终保护、分区和通知裁决，但默认不发送给模型；评测夹具必须显式冻结这些值。

邀请码、API Key、OAuth 令牌、Cookie、密码、验证码、其他用户数据和系统内部配置永不进入模型。

#### 9.5.2 模型输出候选

| 输出 | 最小结构 | 不能做什么 |
|---|---|---|
| 简体标题与摘要 | 忠于原文的短标题、发生了什么、影响对象；事实子句带 `claim_refs` | 不补写邮件没有的行动、日期、金额或后果；高影响子句不得脱离 claim 单独出现 |
| 主题候选 | 9 类主题的数组，每项带证据 | 不能用主题直接决定优先级 |
| 适用性候选 | 范围、原因、`applicability_claim_id`、邮件 `evidence_ids[]`、受信画像 `profile_field_refs[]`、缺口 | 不能把语义相似直接当作已选课程；每个画像引用必须保留来源、确认状态和有效期 |
| 事实声明 | `claim_id`、类型、声明文本、是否高影响、`evidence_ids[]` | 每个高影响语句必须独立成 claim，不能只给整段摘要一个引用 |
| 证据候选 | `evidence_id`、正文段落/字符范围或附件页码、最小逐字片段 | 不能使用不存在、来自旧引用或与声明主体/条件不一致的片段 |
| 行动候选 `actions[]` | 执行者、动作、对象、条件、材料、`mandatory / conditional_mandatory / recommended / optional` 义务、`condition_status`、条件文本 `condition_claim_refs`、用户是否满足条件的 `condition_basis_refs[]`、行动 `claim_refs` | AI 建议必须与邮件写明的行动分开；无邮件行动时返回空数组 |
| AI 管理建议 `management_suggestions[]` | 低风险个人安排建议、建议理由、所依据的已验证 `claim_refs` | 不能建议付款、提交证件、输入凭证、修改账号或其他高风险外部动作；不能进入 `actions[]`、触发“要处理”或获得日历资格 |
| 日期候选 | 原始表达、日期角色、归一候选、时区、冲突、`claim_id` 与 `evidence_ids[]` | 不能决定日历资格，也不能让结束时间冒充开始时间 |
| 关键变更候选 | 被改变字段、新旧值、关联旧事项、`claim_id` 与 `evidence_ids[]` | 不能自行覆盖没有匹配依据的旧事项 |
| 后果候选 | 高/中/低/未知、具体影响、`claim_id` 与 `evidence_ids[]` | 不能用空泛“很重要”替代后果 |
| 安全风险候选 | 风险类型、触发内容、建议核验方式、`claim_id` 与 `evidence_ids[]` | 不能认证来源或建议提交凭证 |
| 不确定项 | 缺什么、冲突在哪里、受影响结论 | 不得用要求用户确认代替暴露缺口 |

模型不得输出最终 `home_section`、最终 `notification_channel`、`official_verified=true`、`calendar_eligible=true` 或任何外部工具调用。

义务四态定义如下，不能把“有按钮/有 CTA”直接等同为强制行动：

| 义务 | 定义 | 对首页分区的含义 |
|---|---|---|
| `mandatory` | 原文明确要求所有适用对象必须执行 | 适用性与来源安全通过后可以进入“要处理” |
| `conditional_mandatory` | 原文只要求满足明确条件的对象必须执行 | 必须同时输出 `condition_status=met / unmet / unknown`；条件文本由 claim/evidence 证明，用户是否满足条件由带来源与确认状态的 `condition_basis_refs[]` 证明。仅已确认受信画像/业务事实支持的 `met` 可进入“要处理”；只有邮件写了条件但没有用户满足依据时必须为 `unknown` 并进入“优先阅读”；`unmet` 不成为当前用户行动 |
| `recommended` | 邮件来源建议执行，但没有强制义务或不执行后果 | 作为“邮件建议”展示，不进入“要处理” |
| `optional` | 自愿报名、可选申请或纯选择性 CTA | 作为“可选行动”展示，不进入“要处理” |

`actions[]` 只承载邮件自身写明的四态行动；`management_suggestions[]` 只承载 AI 基于已验证事实给出的低风险个人管理建议。两者在 Schema、UI、指标和日志中都必须分开。

#### 9.5.3 字段级确定性

用户只看到三态，不展示模型自报的 0–100 分：

| 状态 | 判定条件 | 产品行为 |
|---|---|---|
| 已确认 `confirmed` | 当前字段所需的证据与先决条件全部满足，不存在未解决冲突，Harness 校验通过 | 可以用确定语气展示；日期仍需通过日期角色与时区校验才能获得日历资格 |
| 可能 `possible` | 有支持线索，但该字段所需的适用范围、来源、日期、附件或画像条件不完整 | 展示“可能”及具体缺口；至少进入优先阅读时不得强迫用户先回答 |
| 无法确认 `unconfirmed` | 该字段本应判断，但没有足够证据或存在实质冲突 | 不发布该确定结论；保留原文、已知事实和失败原因 |

字段在该邮件类型中没有意义时统一使用内部枚举 `not_applicable`，不计作 `unconfirmed`；有充分证据确认邮件不适用于当前用户时，输出 `applicability=not_applicable + fact_state=confirmed`。其他字段先决条件按类型判断：内容事实要求逐字存在且语义支持；用户适用性要求受众条件与已确认画像匹配；来源可信度与安全只使用 Harness 的独立认证/域名/链接事实；日期要求原文证据、语义锚点、日期角色和时区校验；更新/取消要求稳定的旧事项匹配。主题等低影响内容事实不因用户画像缺失而自动降级。

#### 9.5.4 Harness 放行与降级

- 证据校验同时要求“引用逐字存在”和“引用语义支持结论”；只满足前者不能放行高影响行动。
- 一个字段失败只阻止该字段及其依赖能力，安全的标题、原文事实、原生重要信号和具体缺口可以继续展示。
- 行动没有证据时，删除该行动候选，不能把整封邮件伪装成已成功提炼。
- 日期歧义、冲突、缺年份或角色不明时，保留原始表达并禁止确定日历资格；只有用户主动尝试创建提醒时才询问如何采用候选日期。
- 适用性只能在明确受众条件与已确认画像匹配时成为“已确认”；`candidate/expired` 课程最多支持“可能相关”；`removed` 不能作为当前课程相关性的正向证据，只能作为负向或历史上下文。邮件自身另有项目、届别、全校或高后果依据时，只由那些依据决定分区。
- 来源可信度由独立来源规则裁决；感叹号、官方口吻、学校 Logo 或相似域名不能证明官方可信。
- 模型整次失败时，合成演示可显示明确标记的预置结果；未来真实邮件只显示原文、原生信号、安全状态和重试进度，不生成预置行动或日期。
- 每张卡记录模型版本、Prompt/Schema 版本、输入数据类别、字段证据、Harness 裁决理由和最终来源模式；不得记录凭证或真实正文到普通日志。

字段失败后，Harness 必须按下表重算依赖结果：

| ID | 失败或未确认字段 | 必须同步阻止/重算 |
|---|---|---|
| DEP01 | 适用性 | 强制行动对当前用户的结论、“要处理”、行动类即时推送和日历资格；可保留“可能相关”与原文行动描述 |
| DEP02 | 行动或义务属性 | 摘要中的“你必须做”、要处理分区、行动提醒、日历资格；AI 管理建议仍须单独标识 |
| DEP03 | 日期角色/归一值 | 截止紧迫性、基于时间的即时推送和日历资格；有证据的无日期行动仍可保留 |
| DEP04 | 来源可信度/安全 | 高风险官方行动、可点击高风险链接、“要处理”和日历资格；改为“优先阅读 + 先核验/安全风险” |
| DEP05 | 证据支持 | 对应 claim 及所有依赖摘要、行动、日期、后果和链接 |
| DEP06 | 更新/取消关系 | 不得覆盖旧事项或继承旧日历关系；以独立可能更新展示 |
| DEP07 | 附件解析 | 所有只存在于附件的 claim；正文独立支持的字段可继续展示 |
| DEP08 | 后果等级 | 高后果保护下限、首页内排序和基于后果的即时通知；未知后果不得被当作低后果降级 |
| DEP09 | 关键变更 | 关键变更驱动的“优先阅读”、即时提醒和旧事项重新置未读 |
| DEP10 | 原生重要信号或来源 | 对应来源的首次保护、星标持续保护、突出标签和摘要覆盖；缺失不能伪造为 `false`，也不能影响其他来源的真实信号 |

#### 9.5.5 固定追问输出契约

三个固定问题分别返回 `answer`、引用的 `claim_ids`、`evidence_ids`、`unknowns` 和来源模式。回答只能重组当前已验证卡片和当前通知证据：

- “我具体要做什么”必须保留义务和条件状态；没有学校强制行动但邮件含 `recommended/optional` 时回答“没有学校强制行动；邮件另有建议/可选行动如下”；连建议/可选行动也没有时才回答“无需操作，仅需知晓”。AI 管理建议始终另列。
- “截止时间依据是什么”必须给出原始日期、日期角色和证据；无确定日期时不得提供归一截止。
- “哪些地方仍不确定”必须列具体缺口及其阻止的能力，不能要求用户替 AI 猜事实。
- 追问不得新增高影响 claim、改变来源认证、首页分区、日历资格或调用外部工具。

## 10. 信息架构与关键页面

### 10.1 邀请入口

- 产品价值一句话；
- 合成数据/试点边界说明；
- 邀请码输入；
- 错误与重试提示。

### 10.2 行动中心

- 默认首页；
- 首页标题表达“先看与你最相关、最不能错过的事”；
- 只按“要处理 → 优先阅读 → 其他通知”三个主区分段；一般通知默认折叠或进入摘要；
- 提供“专业与课程、缴费与资助、注册与学籍、签证与身份、考试与成绩、账号安全、校园活动、住宿与校园生活、其他校务资讯”等统一主题筛选；主题筛选不改变高后果保护下限；
- 每张卡或详情页都提供可选的“与我无关 / 分得太高 / 分得太低”纠错；纠错当前项不阻断阅读，长期规则另行确认；
- 卡片突出“为什么放在这里、是否要做、何时前做什么”；
- “你的行动”区域是可选的；没有学校强制行动但邮件含建议/可选行动时，显示“没有学校强制行动”并单列“邮件建议/可选行动”；完全没有邮件行动时才显示“无需操作，仅需知晓以下变更/信息”。AI 管理建议使用独立区域，不能用空待办或“请确认已读”制造交互；
- 卡片必须展示“与你的专业相关”“课程代码命中”“适用于 2026 届”“全校通知”等相关性标签和依据；
- 对信息不足的邮件直接显示“日期未写清”“适用范围未写清”“附件暂未解析”或“暂未发现明确行动”，不使用笼统的强制“请确认”；
- 列表卡最多突出行动、主题、时间/可信状态三个标签，避免标签淹没结论；
- 不把长篇翻译放在第一屏。
- 已确认无需强制行动、且没有未决高后果或安全缺口的优先阅读卡，在用户打开核心内容后自动折叠到已读可查，不要求再点“完成”；可能适用的高后果通知、附件未解析或来源待核验事项保持活跃，但不强迫用户回答。

### 10.3 通知中心

- 显示学校、发件人、主题、时间、语言、处理分区和分析状态；
- 支持进入当前通知的行动卡；
- 当前版本只展示合成通知。

### 10.4 证据详情

- 证据、原文、简体翻译三个页签；
- 证据与行动/日期明确关联；
- 不确定项与风险不可隐藏。

### 10.5 阶段指南

- 按阶段和主题排序；
- 显示来源、更新时间和有效状态；
- 高风险指南突出官方入口和复核提示。

### 10.6 我的事项与处理记录

- 当前版本只显示页面内模拟状态；
- 未来分别展示稍后、已安排、已完成、与我无关和历史记录；
- 不把“已同步日历”显示成“学校任务已完成”。

### 10.7 个人设置

- 当前展示合成画像和模型状态；
- 未来首次只要求学校、专业/培养项目和入学届别，课程可跳过；设置页管理学期、课程候选/已确认/过期状态、阶段、通知偏好、邮箱连接、日历连接和数据删除。

### 10.8 关键状态机

| 对象 | 当前 v0.1 | 未来邀请制试点 |
|---|---|---|
| 账户 | 未登录 → 邀请验证中 → 本地会话 → 会话过期/登出 | 未获邀 → 邀请通过待验证 → 验证中/过期 → 活跃 → 冻结/删除中/已删除 |
| 邮箱连接 | 不存在 | 未连接 → 授权中 → 首次同步 → 健康/延迟 → 需重连 → 已断开；用户取消和管理员拒绝单列 |
| 通知/行动卡 | 未分析 → 分析中 → DeepSeek 结果/预置结果/安全策略结果 → 已验证/信息不全/字段被阻止 | 已发现 → 处理中 → 已验证/信息不全/AI 暂不可用 → 用户修正 → 被更新/取消/失效 |
| 阅读状态 | 未读 → 已读 | 未读 ↔ 已读；实质更新或稍后到期重新置为未读 |
| 管理状态 | 活跃 → 稍后/模拟已安排/与我无关 | 活跃 ↔ 稍后；活跃 → 已安排/已完成/与我无关；只有用户可改变，更新可按规则恢复活跃 |
| 事项事实状态 | 有效 | 有效 → 已取消/已失效；实质更新创建新的当前版本并把旧版本标为已替代。仅超过截止时间显示“已逾期”，不自动失效 |
| 日历 | 无资格/可预览 → 模拟加入/撤销 | 未授权/无资格 → 可预览 → 已编辑待确认 → 写入中 → 成功/未知对账/失败 → 建议更新/已撤销 |
| 指南 | 当前阶段 → 稍后/页面内模拟完成 | 未到阶段 → 当前 → 稍后/完成 → 过期下架/来源异常 |
| 通知投递 | 不存在 | 待决策 → 即时/摘要/抑制 → 投递中 → 接受/失败/重试 → 用户打开 |

### 10.9 首次使用流程

当前合成版：邀请码 → 明确说明“完全合成、不接邮箱” → 展示合成学校/项目/届别/课程画像 → 用示例卡解释三个首页分区 → 进入行动中心。不得出现真实 OAuth、个人邮箱建账或真实课程输入。

未来真实试点：邀请码验证资格 → 个人邮箱建立账户 → 一页说明 AI 能做什么、不能做什么和数据边界 → 只填写学校、项目/专业和届别 → 课程可跳过 → 采用可修改的推荐通知设置 → 用户主动连接学校邮箱 → 显示首次同步进度和最后同步时间 → 进入行动中心。课程候选只在设置页集中确认。

### 10.10 四套状态：三条主轴与派生可见性

阅读、用户管理和事项事实是三个正交的事实源；列表可见性是由三条主轴、去重关系和用户归档事件计算的第四套可重算投影。实现可以为查询性能物化该投影，但不得把它变成与事实源竞争的第二套真相；禁止把这些语义塞进一个“已处理”枚举。

| 状态轴 | 枚举 | 谁改变 | 准确含义 |
|---|---|---|---|
| `read_status` | `unread / read` | 系统根据真实打开事件；实质更新或稍后到期可重置 | 只表示是否看过核心内容 |
| `management_status` | `active / snoozed / arranged / completed / irrelevant` | 用户动作；系统只能按已授权规则恢复 `active` | 用户准备怎样管理，不表达邮件事实 |
| `item_status` | `active / cancelled / invalidated` | 经验证的新邮件或官方事实 | 当前事项是否仍有效，不表达用户是否看过；超过截止时间本身不能置为 `invalidated` |
| `visibility_status` | `active / read_folded / user_hidden / merged` | 由上述状态、去重关系和用户归档事件计算，可物化但必须可重算 | 是否出现在活跃首页，不是第四个处理分区或独立事实源 |

版本关系另用记录级元数据 `version_status=current/superseded`：实质更新创建一个新的 `current` 版本，旧版本改为 `superseded`；当前版本的 `item_status` 仍为 `active`，除非新事实明确取消或使其失效。版本元数据不构成第五条用户生命周期轴。

关键行为：

- 只有“已确认无需强制行动且不存在未决高后果、安全、适用性、来源或附件缺口”的优先阅读卡，在用户真正打开核心结论后才自动变为 `read_folded`，进入已读可查列表；不要求点击“完成”，也不计为已管理。用户星标仍保留，并可在星标筛选中找到。
- 可能适用的高后果通知、附件未解析或来源待核验事项即使已经阅读也保持 `visibility_status=active`；用户仍可主动设为稍后、与我无关或归档，系统不增加强制确认队列。
- “要处理”卡即使已经阅读仍保持 `visibility_status=active`，直到用户设为稍后、已安排、已完成或与我无关。经验证的取消/失效会创建一条新的未读关键变更版本；该变更在用户打开前仍保持活跃，不能因为旧事项已经取消而直接消失。
- `snoozed` 到用户选择的时间后恢复 `active + unread`；它只记为弱管理动作，不自动变成 `arranged`。
- `arranged` 只在持久内部提醒/任务创建成功，或未来真实日历经用户批准写入成功后成立；合成版只能记录 `simulated_arranged` 事件，不改变真实指标。
- `completed` 只能由用户主动声明，表示用户自报完成；日历同步、提醒送达、已读和打开官方入口均不能自动完成。
- `irrelevant` 默认只作用于当前事项；建立长期忽略规则需要再次确认，且不能突破高后果、安全、强制行动和关键变更保护。
- 重要日期、地点、金额、适用对象或行动变化时，把旧版本设为 `version_status=superseded`，创建 `version_status=current + item_status=active` 的新版本，并设置 `read_status=unread`；若变化使既有安排失效或产生新义务，则管理状态恢复 `active`，否则保留原管理状态。
- 截止时间已过只计算 `due_status=overdue` 并显示“已逾期/请核验补救方式”；未完成的强制或高后果事项继续保持活跃。邮件或官方事实证明事项已取消/不再可处理时，设置 `item_status=cancelled/invalidated`，创建未读关键变更并保持 `visibility_status=active` 直到用户打开；打开后才自动折叠到已读历史。该过程不改写用户历史，也不自动变成完成。
- 非实质性文案变化继承全部状态，不重复提醒。已有日历事件时，更新或取消只生成变更预览，不能静默修改或删除。

指标口径：创建有效稍后只算“弱管理”，单独报告，不进入核心北极星分子；`arranged`、用户主动 `completed` 或未来另行定义的可验证持久管理动作才计入核心“已管理”。打开官方入口只算弱行动。

### 10.11 状态迁移真值表

以下 `ST01–ST12` 是 Harness 的锁定确定性序列。每个序列必须冻结初始四套状态、触发事件、用户动作来源、预期四套结果、`due_status` 和是否产生通知；不得只验最终页面文案。

| ID | 初始条件与事件 | 预期结果 |
|---|---|---|
| ST01 | 信息完整、已确认无需强制行动且无未决高后果/安全缺口的优先阅读卡被打开 | `read=read`，管理与事项事实不变，`visibility=read_folded`，不计核心管理 |
| ST02 | 可能适用的高后果、附件未解析或来源待核验卡被打开 | `read=read`，`visibility=active`，其他状态不变，不强迫确认 |
| ST03 | “要处理”卡被打开 | `read=read`，`management=active`，`visibility=active` |
| ST04 | 用户为活跃事项设置合法稍后时间 | `management=snoozed`，`visibility=user_hidden`，记录弱管理；事项事实不变 |
| ST05 | ST04 的稍后时间到达 | `management=active`、`read=unread`、`visibility=active`，按用户选择的方式提醒一次 |
| ST06 | 持久内部安排成功，或未来经批准的真实日历写入成功 | `management=arranged`、`visibility=user_hidden`；不改变 `item_status`，不自动完成 |
| ST07 | 用户主动标记完成 | `management=completed`、`visibility=user_hidden`；保留事实和操作历史 |
| ST08 | 用户把当前事项标为与我无关 | `management=irrelevant`、`visibility=user_hidden`；不自动建立长期规则 |
| ST09 | 已安排/已完成事项收到会使原安排失效或产生新义务的实质更新 | 旧版本 `superseded`；新版本 `current + item=active`、`read=unread`、`management=active`、`visibility=active`，重新裁决通知 |
| ST10 | 已安排事项收到实质更新，但不影响义务或既有安排 | 旧版本 `superseded`；新版本 `current + item=active`、`read=unread`、保留 `management=arranged`、`visibility=active` 供用户阅读 |
| ST11 | 来源事实明确取消或证明事项不再可处理 | 创建关键变更版本，`item=cancelled/invalidated`、`read=unread`、`visibility=active`；保留管理和日历历史，只生成变更预览。用户打开后转为 `read_folded`，无需手工确认 |
| ST12 | 截止已过但没有取消/失效证据，且强制或高后果事项未完成 | `due_status=overdue`、`item=active`、`visibility=active`；显示补救核验提示，不自动完成 |

## 11. 数据与隐私要求

### 11.1 当前 v0.1

- 只处理 6 封合成邮件和一个合成画像；
- 不提供上传、URL 抓取、开放文本框或真实邮箱入口；
- 会话只存在内存；
- 不持久化真实学生数据；
- DeepSeek Key 只在服务端本地环境变量中；
- 普通日志不得包含 Key、邀请码、邮件正文或请求授权头；
- 应用只监听本机，不部署。

### 11.2 未来真实试点

| 数据等级 | 示例 | 要求 |
|---|---|---|
| S0 凭证 | 会话密钥、OAuth 令牌 | 密钥管理、最小访问、绝不进模型或普通日志 |
| S1 原始内容 | 邮件、附件、解析文本 | 加密、租户隔离、短期处理、禁止训练 |
| S2 派生业务数据 | 行动卡、日期、证据、纠错 | 字段权限、版本审计、支持删除 |
| S3 用户配置 | 学校、课程、阶段、偏好 | 可查看、可修改、可导出 |
| S4 遥测 | 延迟、错误、成本 | 去标识化，不记录正文和凭证 |

真实研究样本不得整箱采集；只能由参与者主动选择单封邮件或相关线程。真实身份证、护照、签证扫描件、银行卡、医疗材料、密码和验证码禁止采集。

## 12. 失败与降级

| 场景 | 产品行为 |
|---|---|
| 未配置或无法调用模型 | 合成演示明确展示预置分析；未来真实邮件只展示原文、原生信号、安全状态和重试进度 |
| 模型超时或截断 | 有限重试；仍失败则按数据类型降级，不把预置事实套到真实邮件 |
| 输出结构不合法 | 不发布模型候选；安全的原文事实和具体失败状态仍可展示 |
| 行动或日期缺少证据 | 阻止对应结论发布 |
| 日期歧义或冲突 | 展示原文和疑点，禁止日历资格 |
| Prompt Injection/钓鱼 | 忽略越权指令，执行安全策略 |
| 不支持附件 | 明确提示未解析，不能猜测 |
| 邮箱同步延迟 | 未来必须显示最后同步时间和降级状态 |
| 日历写入失败 | 未来明确显示失败，不显示为已同步 |
| 指南无来源或已过期 | 不发布为当前确定事实 |

## 13. 成功指标

### 13.1 当前 v0.1 验收

- 6 封合成通知均能进入完整产品闭环；
- 英文、繁体、日期更新、普通资讯和安全攻击样本均有明确预期；
- 至少一个合成样本完成真实 DeepSeek 固定结构输出 + Harness 校验；
- 模型与预置结果来源标识正确率 100%；
- 无 Key 泄露、无真实邮箱入口、无真实日历写入、无部署配置；
- Prompt Injection 不能改变身份、权限、工具或日历资格；
- 自动化测试和安全边界测试全部通过。

以上只能证明原有 6 封合成演示与单样本模型链路；不能证明 v0.2 新增优先级、课程画像和生命周期规则已经实现或通过验收。

### 13.2 v0.2 合成产品验收夹具

本夹具用于在不寻找真实学生、不接邮箱和不处理真实数据的前提下，验证实现是否符合 v0.2 产品合同，并方向性观察 AI 相对规则基线的增量。单人确认的夹具不能称为独立金标、不能证明统计优势，也不能证明真实用户需求、真实邮件准确率或线上稳定性。

首版固定为 **48 个完全虚构的产品验收案例**，发件地址使用 `.invalid` 域名：

| 主场景 | 数量 | 必须覆盖 |
|---|---:|---|
| 专业与课程直接相关 | 8 | 课程代码/别名、项目、届别、相似名称但不适用、相关但无行动 |
| 高后果非专业事务 | 8 | 缴费、注册、签证、学籍、考试资格、账号安全 |
| 原生重要标记边界 | 6 | 发件人重要、供应商重要、用户星标、无行动、与用户无关、重要钓鱼 |
| 普通行政、活动与生活 | 5 | 自愿活动、一般资讯、住宿与校园生活 |
| 画像缺失与适用性歧义 | 5 | 缺课程、候选/过期/已移除课程、届别冲突、项目别名、全校通知 |
| 日期、更新、取消与去重 | 8 | 清晰截止、相对日期、无年份、开始/结束混淆、逾期不等于失效、延期、取消、重复 |
| 安全与对抗内容 | 4 | Prompt Injection、索取验证码、伪造链接、合法邮件引用攻击文本 |
| 附件与解析状态 | 4 | 预置合成附件文本/页码、正文与附件冲突、加密、损坏或不支持状态；不实现文件上传或真实 PDF 解析 |
| **合计** | **48** | 同时覆盖 4.2.1 的全部冲突场景 |

交叉切片至少包含：英文 20、繁体 14、混合语言 10、简体中文 4；要处理 16、优先阅读 18、其他通知 10、无新首页卡而合并 4；至少 12 个原生重要信号，且发件人、供应商、用户星标各至少 4 个；24 个业务日期；8 个安全/可疑案例。8 个源邮件分别搭配 2 个不同合成画像，形成 16 个案例并包含在总数 48 内。

每个案例必须包含：案例 ID；可空的 `matrix_row_id`；输入邮件与合成画像；`sent_at/received_at`；画像状态；原生重要信号；`first_seen`；当前星标；既有纠错/长期规则；预期保护结果；`source_status`、`action_channel_status` 及命中的 `source_truth_id`；主题；适用性；原子 `actions[]`、义务、`condition_status`；独立 `management_suggestions[]`；日期角色与归一结果；claim/evidence 映射；后果、`unknown_with_high_consequence_clue` 与 `consequence_sort_bucket`；不确定项；每个候选字段的预期 `fact_state=confirmed/possible/unconfirmed`；`incoming_disposition`、命中的 `relation_truth_id`；`resulting_item.home_section`；`north_star_eligible` 与适用时的 `north_star_maturity_status`；字段放行/拦截；更新关系和日历资格。不要求摘要逐字一致，但人物、动作、否定、条件、金额、日期和义务必须一致。

数据集分为 32 个可见开发/回归案例和 16 个锁定回归案例；同源邮件、线程和画像变体必须进入同一组。16 个锁定基础案例的 `matrix_row_id` 必须分别且唯一地取 `M01–M16`，不重不漏；M16 同时验收 `incoming_disposition=update_existing` 与更新后原事项 `resulting_item.home_section=优先阅读`。任何锁定答案被用于修改 Prompt、规则或代码后，该案例转入开发集，并补入从未看过答案的新锁定案例，不能只改版本号继续冒充盲测。

另设 **16 个锁定变异子案例**，不增加 48 个基础邮件数：`candidate / expired / removed` 课程各一个单变量画像变异；9.5.4 的 `DEP01–DEP10` 各一个字段失败变异；更新、延期、取消各一个关系变异。每个变异只改变一个根输入或根校验结果，必须记录 `parent_case_id`、`mutation_id`、可空的 `dependency_id`、`root_changed_input` 和可包含多个下游变化的 `expected_dependency_deltas[]`；`DEP01–DEP10` 必须分别且唯一出现一次，不重不漏。

另设 12 个固定追问子案例，不增加基础邮件总数：三个问题各 4 个，每个问题固定 2 个开发追问和 2 个锁定追问；6 个开发追问只作可见回归，6 个锁定追问各独立运行 3 次。验收 `answer`、claim/evidence 引用、不确定项、义务/条件保持和“不得新增高影响事实”；锁定追问任一次出现高影响硬失败即不通过。

另设 12 个锁定状态迁移序列，逐一对应 10.11 的 `ST01–ST12`，不增加基础邮件数。每个序列必须记录完整初始状态、事件、预期四套状态、`due_status`、通知副作用和指标事件；它们由固定 Harness 策略确定性运行，不参与 AI 增量比较。

确定性规则基线只允许使用原生重要标记、发件人/主题/受控域名规则、课程与项目精确匹配、关键词表、日期/金额/课程代码正则和固定分区表；不得调用模型、读取预期答案或使用预置行动卡。规则版本必须在查看锁定答案前冻结；AI+Harness 与规则基线读取相同输入。通知渠道由 Harness 使用固定香港当前时间、默认偏好和免打扰关闭的确定性规则单独验收，不计入模型增量。

产品合同绝对硬门在 16 个锁定基础案例、16 个锁定变异子案例和 6 个锁定追问的每次锁定运行中都必须满足：

- `M01–M16` 映射不重不漏，`incoming_disposition` 与 `resulting_item.home_section` 完全一致率 100%；
- 原生重要信号来源与应展示保护结果 100%；
- 高影响结论证据覆盖、claim/evidence 映射和字段依赖降级正确率 100%；
- 所有高影响候选字段的 `fact_state` 正确率 100%，把 `possible/unconfirmed` 错误升级为 `confirmed` 的次数为 0；
- `actions[].obligation` 四态正确率 100%；`recommended/optional` 错升为强制，以及 `mandatory/conditional_mandatory` 错降为建议/自愿，均为 0；
- 条件强制的 `condition_status`、条件文本证据和 `condition_basis_refs[]` 正确率 100%；只有邮件条件文本而没有已确认用户条件依据时判为 `met`，或 `unknown/unmet` 错误进入“要处理”，均为 0；
- `DEP01–DEP10` 变异映射不重不漏，全部 `expected_dependency_deltas[]` 正确率 100%；
- 虚构高影响行动、错误确定关键日期、越权、错误日历资格均为 0；
- 高后果事项错误降入其他通知、高风险来源未验证邮件错误进入要处理、`official_verified` 误判为真均为 0；
- `north_star_eligible` 与适用时的 `north_star_maturity_status` 必须符合 2.3，取消/失效或截止更新不得回写历史资格、重复计数或追溯美化分母；
- `candidate / expired / removed` 课程单独导致“要处理”为 0；
- 更新/取消后继续把旧事实显示为当前确定事实为 0；
- 仅因截止已过就把未完成强制/高后果事项判为失效或移出活跃首页为 0；
- `management_suggestions[]` 错误进入邮件 `actions[]`、“要处理”或日历资格，或建议高风险外部动作，均为 0；
- 预置结果冒充 DeepSeek 为 0；
- 非高影响错误每次运行最多 2 个，错误单位固定为“一个预期原子输出单元与结果不一致”：一个多标签主题的单个标签或一个低影响 claim 各计 1 个，同一单元的重复措辞问题不重复计数；任何此类错误都不能改变分区、通知、安全或用户义务；
- 6 个锁定追问不得新增高影响 claim，证据引用、条件/义务和“不确定”回答符合契约；6 个开发追问只报告回归结果，不并入锁定硬门。

固定模型、Prompt、Schema、Harness 策略和解码参数后，16 个锁定基础案例、16 个锁定变异子案例和 6 个锁定追问各独立运行 3 次；任何一次出现高影响硬失败即不通过。开发案例与开发追问可反复用于调试，但不得计入锁定通过率。

作为另一组绝对硬门，`SRC01–SRC07` 来源/行动渠道、`REL01–REL07` 事项匹配、`CON01–CON06` 后果/排序和 `ST01–ST12` 状态迁移属于确定性 Harness 真值表：每个策略版本至少完整运行一次且必须 100% 通过，其中 `ST01–ST12` 的四套状态、逾期处理、通知与指标事件必须完全一致；它们不因模型重复运行而重复计数，也不进入 AI 相对规则基线指标。

AI 增量只在预先声明的锁定 `semantic_difficult` 子集上方向性报告，错误单位固定为“适用性判断 FP/FN + 原子行动 FP/FN”。开发集不参与计算。若规则基线错误至少 5 个，双人独立复核后才可正式要求 AI 错误数至少下降 30%、至少 3 个源案例净改善且无高影响回归；若基线错误不足 5 个，只能结论为“评测包不足以证明 AI 增量价值”，应补更难的新锁定案例。单人夹具只报告逐例原始差异，不作正式 AI 优势结论。

正式双人金标、每切片至少 20、英文/繁体各 50 和 PDF 至少 20 个锁定案例仍是未来真实邀请制试点质量门，不适用于本 48 例产品夹具。本次批准只授权把验收要求写进 PRD，不授权创建或运行 48 个基础案例、16 个锁定变异子案例、12 个固定追问、12 个状态迁移序列或确定性真值表，也不授权调用 DeepSeek 或修改代码。

### 13.3 未来试点业务指标

以下数值已随本 PRD 整体批准，作为未来正式试点基线；不构成真实试点启动授权：

| 指标 | 建议目标 | 假设未验证线 |
|---|---:|---:|
| 重要事项截止前管理率 | ≥60% | <40% |
| 重要事项行动卡查看率 | ≥80% | <60% |
| 已查看事项中的管理行动率 | ≥75% | <50% |
| 30 秒核心理解成功率 | ≥85% | <70% |
| “无需行动”正确理解率 | ≥90% | <80% |
| 新邮件到行动卡 5 分钟内完成率 | ≥95% | <90% |
| 内部发现后处理延迟 | p50 ≤90 秒、p95 ≤4 分钟 | p95 >8 分钟时停止扩大 |
| 通知服务接受率 | ≥98% | <95% |
| 有效审批后的日历写入成功率 | ≥99% | <97% |

“30 秒核心理解成功”要求用户在首次打开卡片后 30 秒内正确回答：发生了什么、是否需要本人行动、何时、为什么与自己相关；没有明确行动时必须回答“只需知晓”，不能把 AI 建议误解为学校要求。北极星至少观察 6 周且成熟分母达到 50 项后结算；样本不足时只报告数量，不宣称达标。

### 13.4 未来 AI 质量门

以下数值已作为未来试点推荐基线获批；只有真实评测集和测量方法另行通过评审后才用于准入：

| 指标 | 试点目标 |
|---|---:|
| 学校邮件识别精确率/召回率 | 均 ≥95% |
| 用户适用性精确率/召回率 | 均 ≥95% |
| 专业/课程直接相关识别精确率/召回率 | ≥95% / ≥98% |
| 多主题标签准确率 | ≥95% |
| 原生重要标记来源保留率 | 100% |
| 用户星标在取消前至少进入优先阅读覆盖率 | 100% |
| 发件人/供应商重要首次且无已批准降级规则时至少进入优先阅读覆盖率 | 100% |
| 重要可行动邮件召回率 | ≥98% |
| 高后果事项错误降入其他通知 | 0 |
| `official_verified` 错误判真 | 0 |
| 来源未验证/可疑高风险行动错误进入要处理 | 0 |
| 候选、过期或已移除课程单独导致要处理 | 0 |
| 字段失败后的依赖降级正确率 | 100% |
| 专业/课程重要时间变化漏判 | 0 次造成用户错过关键安排 |
| 行动项精确率/召回率 | ≥95% / ≥92% |
| 确定日期全字段准确率 | ≥99% |
| 歧义/冲突日期被识别并阻止确定行动的召回率 | ≥97% |
| 高影响结论证据覆盖率 | 100% |
| 证据支持正确率/可回源率 | ≥98% / ≥99% |
| 英文→简体事实保真率 | ≥95% |
| 繁体→简体事实保真率 | ≥98% |
| PDF 关键字段与页码证据忠实率 | ≥98% |

### 13.5 成本基线

本 PRD 已批准采用以下质量与成本平衡基线；采购和真实调用仍需另行批准：

- 自动处理单封邮件平均变量成本 ≤¥0.30；
- p95 ≤¥0.60；
- 单次自动运行 >¥2 时熔断并审计；
- 活跃连接用户月变量成本 ≤¥20；
- 无效重试成本占比 ≤10%。

当前合成数据 smoke 的费用只用于技术验证，不代表生产成本已经达标。

## 14. 硬阻断项

以下目标必须为 0；出现一次即暂停相应能力或全部试点：

- 跨用户/跨租户访问；
- OAuth 令牌、Magic Link、明文邀请码或 API Key 泄露；
- 无用户审批的外部日历写入；
- 相同幂等键创建重复日历事件；
- Prompt Injection 改变身份、权限或工具边界；
- 虚构缴费、注册、考试等高影响行动；
- 日期、金额、否定或必须/禁止含义被关键翻译反转；
- 关键日期被错误包装成确定事实；
- 真实邮件被发送给未经批准的模型供应商；
- 未关闭的严重或致命安全问题。

推荐暂停策略：跨租户、凭证泄露和未授权写入暂停全部试点；日期、翻译、重复日历或未造成全局后果的 Agent 越权先暂停受影响能力。

## 15. 试点门禁

### 15.1 当前合成数据版完成门

- 需求、边界、Agent/Harness 分工和验收口径已写入 PRD；
- 合成数据闭环与真实模型单样本 smoke 有可审计结果；
- 不因本地成功而声称真实邮件、OAuth、线上稳定性或产品价值已验证。

### 15.2 宣称 v0.2 行为已实现前

- 48 个合成产品验收夹具完成独立文件化、32/16 分组和版本记录；单人夹具不称独立金标；
- DeepSeek+Harness 与非模型规则基线在相同输入上完成对照；
- 13.2 的全部合成硬门通过；
- 优先级冲突矩阵、渐进式课程画像、通知策略和主动管理生命周期具备可测试实现；
- 仍只能宣称“合成场景通过”，不能宣称真实邮件准确率。

### 15.3 进入单用户本地只读影子前

必须另行批准并完成：

- 由目标用户主动选择少量单封邮件或线程，不整箱导出；具体接入方式另行评审；
- 真实样本同意、脱敏、短期保留和删除流程；
- 模型供应商不训练、数据保留和处理区域审查；
- 48 个合成产品验收夹具通过 13.2 硬门；
- 适用于具体接入方式的授权、日志泄露、Prompt Injection 和删除测试；
- 所有 AI 结果 100% 人工复核。

该阶段不要求先取得 240 封真实邮件；它用于发现问题，不用于对外声称准确率，也不开放自动推送、真实日历写入或公开部署。本 PRD 不构成该阶段的真实数据授权。

### 15.4 5–8 人只读影子的启动门与退出门

- **启动门**：试点学校、首发邮箱供应商和第三方 OAuth 可行性有证据；同意、数据最小化、租户隔离、模型供应商和安全流程另行获批；单用户本地影子未出现错误确定关键日期、隐私、越权或高影响虚构；影子期所有 AI 结果继续 100% 人工复核，不开放真实日历写入。
- **退出门**：该阶段累计观察至少 100 封新邮件，其中至少 20 封需要行动、15 封包含日期；不要求在启动前由用户一次性提供。

### 15.5 进入 30–100 人邀请制试点前

- 离线语料至少达到 240 封真实邮件、120 封合成/挑战邮件和 360 个金标案例；推荐达到 300 / 150 / 450 后再对外使用稳定质量口径；
- 同时满足 `02-pilot-evidence-plan.md` 表中的贡献者、学校分布、PDF、重要行动、行动日期和更新线程硬性最低门；5–8 人影子样本只有在同意、脱敏、标注和拆分规则一致时才可计入上述总量；
- 5–8 名种子用户只读影子验证至少覆盖 100 封新邮件；
- 至少 20 封需要行动、15 封包含日期；
- 错误确定日期、隐私事件、越权和跨租户行为均为 0；
- 正式 AI 指标达到试点目标；
- 日历写入必须通过单独审批与幂等验收后再开放。

## 16. 风险与应对

| 风险 | 当前判断 | 应对 |
|---|---|---|
| 用户是否持续需要行动卡 | 未验证 | 先验证行动卡理解效率与管理率，不用功能数量代替需求证据 |
| 无法取得真实样本 | 高 | 合成数据可继续做安全回归，但不冒充真实准确率 |
| 学校阻止第三方 OAuth | 未验证 | 先验证学校与租户政策；禁止索取邮箱密码 |
| AI 错过关键行动或日期 | 高影响 | 证据门、日期校验、人工复核和能力级停机 |
| AI 产品感不足 | 可控 | AI 负责理解与解释，界面明确显示来源；不靠开放聊天框制造 AI 感 |
| 专业/课程画像缺失或过期导致错排 | 高 | 首次只填项目/届别，课程可跳过；AI 集中提出候选，用户确认后生效；不确定时进入优先阅读并显示具体缺口 |
| 发件人滥用感叹号或高重要性 | 中 | 重要标记只提高注意力；来源可信度、安全和行动仍独立校验 |
| 指南过期或错误 | 高影响 | 来源、版本、审核人与有效期门禁 |
| 模型延迟或成本过高 | 未验证 | 有限重试、缓存、熔断、模型准入和成本指标 |
| 内地网络可用性 | 未验证 | 上线前单独做真实网络与认证链路测试 |
| 用户误以为加入日历等于完成任务 | 可控 | 文案和指标明确区分“管理”与“完成” |

## 17. 已冻结决策

以下内容作为本 PRD 基线：

1. 产品定位为 AI 留学通知行动管家，而不是邮箱聚合器或通用聊天机器人；
2. 首页以行动卡为主，追问附着在具体通知上；
3. 当前模型只接 DeepSeek，不保留 OpenAI 运行路径；
4. 当前只使用合成数据，不接邮箱、不部署；
5. 邀请码必须出现在准入流程；
6. 首期只有一个受限邮件智能 Agent；
7. 高影响事实必须有原文证据；
8. 日期、时区、权限、通知和日历副作用由 Harness 控制；
9. 未来日历写入必须逐次预览确认；
10. 高风险指南必须有来源、版本和有效期；
11. 不使用 Subagent、Agent Teams、Goal Loop 或开放式工具自治；
12. 邮件排序以个人相关性和后果共同决定：专业/课程默认优先于普通学校资讯，但缴费、注册、签证、学籍、考试资格和安全等高后果事项拥有更高优先级。
13. 用户星标在取消前保持“优先阅读”下限；发件人或邮箱供应商重要标记保证首次被总结并至少进入“优先阅读”，但不能单独证明必须行动、官方可信、立即推送或永久置顶。
14. 信息不足默认展示已知事实和具体缺口，不强迫用户逐封确认；确认只用于外部副作用、把歧义升级为确定事实或改变长期规则。
15. “要处理”只用于已确认适用、义务为强制/条件强制且来源安全足以执行的邮件；自愿 CTA、AI 建议和来源未验证的高风险行动不得进入；无行动的课程/考试关键变更进入“优先阅读”，可以即时提醒但不能伪装成待办。
16. 首次画像只要求学校、项目/专业和届别；课程可跳过，AI 只能提出候选并集中供用户确认。
17. 主题、处理分区、后果、来源可信度和信息完整度相互独立；统一使用九类主题标签。
18. 生命周期采用阅读、管理、事项事实和派生可见性四套状态；已读、稍后、日历同步或打开入口均不能自动代表完成。
19. 样本采用分阶段门禁：先做 48 个合成产品验收夹具，再另批单用户本地影子、5–8 人只读影子；240–300 封真实邮件只作为正式邀请制试点和质量口径的门槛。
20. 阶段指南保持 P1 辅助能力，不进入邮件行动中心核心价值的当前完成门。

## 18. 不阻塞当前 PRD、但阻塞真实试点的待确认项

1. 未来首批 2–3 所学校；
2. 首发 Microsoft Graph 还是 Gmail API；
3. 生产模型供应商、数据处理区域与合同条款；
4. 真实生产邮件与附件的具体短期 TTL；
5. 指南内容负责人、审核人和更新 SLA；
6. 邀请码批次、有效期、单码使用次数和发放渠道；
7. 断开邮箱后是否保留用户主动选择的历史派生行动卡；
8. 未来默认目标日历、提醒规则和解绑后的事件处理。
9. 未来是否增加课表导入作为课程画像的可选来源；在决定前采用“课程可跳过 + AI 集中提出候选 + 用户确认”的方案。

这些问题没有答案时，产品可以继续做合成验证，但不能接入真实邮箱、真实学生数据或部署。

## 19. 发布定义

### v0.1 合成数据验证版

满足当前功能与安全验收，证明真实模型与 Harness 的最小闭环，但不对外发布。

### v0.1.1 单用户本地只读影子

仅在真实数据、模型供应商和本地处理方式另行批准后，由目标用户主动选择少量单封/线程进行 100% 人工复核；不自动推送、不写真实日历、不公开部署，也不用于准确率宣传。

### v0.2 只读影子版

在单独取得真实数据与 OAuth 授权后，供 5–8 名种子用户使用；所有 AI 结果人工复核，不开放真实日历写入。

### v1.0 邀请制试点

离线质量、影子验证、安全和成本门全部通过后，面向 30–100 名学生开放；仍不包含自动邮件操作和默认自动日历写入。

## 20. PRD 验收

产品负责人确认本 PRD，代表确认：

- 产品问题、目标用户、核心闭环和当前范围；
- 当前合成数据版与未来真实试点的边界；
- AI 与 Harness 的职责；
- 功能需求、失败降级、数据红线和硬阻断项；
- 推荐的业务、质量和成本基线。
- v0.2 的行为定义是后续设计、实现和测试的唯一产品口径；旧架构文档如有产品措辞冲突，以本 PRD 为准。

确认本 PRD **不代表**批准创建或运行任何验收资产（包括 48 个基础案例、16 个锁定变异子案例、12 个固定追问、12 个状态迁移序列及确定性真值表），也不代表批准修改代码、调用模型、OAuth 注册、真实邮箱、真实学生数据、日历写入、部署或正式采购；这些事项仍需分别通过门禁。

> 批准记录（2026-08-30）：产品负责人已整体批准 PRD v0.2，并单独授权下一阶段生成上述静态合成验收夹具。该新增授权只解除“创建验收资产”门禁，不解除夹具运行、产品代码、模型、邮箱、真实数据、日历或部署门禁。
